{
  "name": "RN-styled-components",
  "version": "0.1.3",
  "description": "Sample extension description.",
  "scripts": {
    "start": "zem start",
    "build": "zem build",
    "clean": "zem clean",
    "exec": "zem exec",
    "publish": "zem publish"
  },
  "zeplin": {
    "displayName": "RN-styled-components",
    "platforms": [
      "web"
    ]
  },
  "dependencies": {
    "zem": "^0.4.3"
  }
}
